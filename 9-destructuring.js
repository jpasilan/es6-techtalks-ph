let person = {
  first: "John",
  last: "Doe",
  age: 25
};

// ES5-way of taking values from objects and assigning to variables.
var first = person.first;
var last = person.last;
var age = person.age;

console.log(first, last, age);
